[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = [
  "pip install torch==2.1.0+cpu torchvision==0.16.0+cpu --extra-index-url https://download.pytorch.org/whl/cpu",
  "pip install -r requirements_app.txt"
]

[start]
cmd = "uvicorn main_v6:app --host=0.0.0.0 --port=$PORT"
